# react-scripts 5.0.1 declares an optional peer of typescript ^3 || ^4, but the
# project needs TypeScript 5 to parse current type definitions. TS 5 builds fine
# under react-scripts, so relax peer resolution rather than pin an unusable
# compiler.
legacy-peer-deps=true
